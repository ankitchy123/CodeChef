#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cout << "Enter t : ";
    cin >> t;
    while (t--)
    {
        long long int n;
        cout << "Enter n : ";
        cin >> n;
        cout << (n / 2) + 1 << endl;
    }

    return 0;
}